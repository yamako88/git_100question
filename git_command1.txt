# gitに馴染む
git config --global user.email "kkk@kk.co.jp"
git config user.name
git config user.email
git help help
git help -a
git --paginate help -a
git help -g
git help glossary

# リポジトリの作り方と使い方
git init
git status
git add .
git commit -m "hoge"
git log
git log --stat
git ls-files

# ファイルの追跡と更新
git diff
git commit -a -m "hoge"
★git diff --staged
git add --dry-run .
git add .
git log --shortstat --oneline

2日目

